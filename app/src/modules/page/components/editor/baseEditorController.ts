import { ref, isRef, type Ref } from 'vue'
import type { EditorControllerContract } from './contract'

export function useTreeController(initialNodes: Ref<TreeNode> | TreeNode): EditorControllerContract {

  return {
 
  }
}